/**
 * Drizzle schema. Domain tables (users, cases, notes, documents, …) are added
 * in later prompts. Kept as an explicit, empty module so the Drizzle client and
 * migration tooling have a stable import target.
 */

export {};
